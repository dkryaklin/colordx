---
"@colordx/core": minor
---

Added support for modern CSS space syntax (e.g., `hsl(0 100% 50%)`, `rgb(255 0 0 / 0.5)`) in HSL and RGB string parsing, including angle units and percentage alpha values.
